PyMuPDF>=1.24
openpyxl>=3.1

# Offline OCR for scanned/image-only PDFs (also needs the system 'tesseract-ocr'
# binary: apt-get install tesseract-ocr). Optional — only the batch --ocr path uses it.
pytesseract>=0.3.10
Pillow>=10.0

# Web app (webapp/) — optional; not needed for the library/CLI
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9
libsql-experimental>=0.0.40   # durable Turso/libSQL backend (falls back to SQLite)
